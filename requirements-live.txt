# Only needed in live mode (order signing). Pinned by version. Before the first live order, replace this
# with a fully hash-locked set of the SDK and every transitive dependency (docs/SECURITY.md → Supply chain).
hyperliquid-python-sdk==0.24.0
